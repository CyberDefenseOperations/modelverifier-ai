# Node
node_modules/
npm-debug.log*
.npm

# Build outputs — generated by npm run build / build:integration
# Do NOT gitignore integration/ as a whole — _headers and static docs are source-controlled.
# Only ignore generated JSON bundles that are rebuilt on every CI run.
integration/controls.json
integration/controls.min.json
integration/baseline.json
integration/framework-index.json

# Editor
.DS_Store
.vscode/
*.swp
*.swo

# Environment
.env
.env.local

# Cloudflare
.dev.vars
wrangler.toml.local
